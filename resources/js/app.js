import './bootstrap';

import { Dropdown, Collapse, Carousel, initTE } from "tw-elements";
initTE({ Dropdown, Collapse, Carousel }, true ); // set second parameter to true if you want to use a debugger
